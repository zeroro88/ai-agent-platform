{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "compile: agent-core",
            "type": "shell",
            "command": "mvn",
            "args": ["-q", "-pl", "agent-core", "compile"],
            "options": { "cwd": "${workspaceFolder}" },
            "group": "build",
            "presentation": { "reveal": "silent", "panel": "shared" }
        },
        {
            "label": "compile: ai-gateway",
            "type": "shell",
            "command": "mvn",
            "args": ["-q", "-pl", "ai-gateway", "compile"],
            "options": { "cwd": "${workspaceFolder}" },
            "group": "build",
            "presentation": { "reveal": "silent", "panel": "shared" }
        },
        {
            "label": "compile: rag-service",
            "type": "shell",
            "command": "mvn",
            "args": ["-q", "-pl", "rag-service", "compile"],
            "options": { "cwd": "${workspaceFolder}" },
            "group": "build",
            "presentation": { "reveal": "silent", "panel": "shared" }
        }
    ]
}

server:
  port: 8083
  servlet:
    encoding:
      charset: UTF-8
      enabled: true
      force: true

spring:
  application:
    name: legacy-dummy
  profiles:
    default: local

logging:
  level:
    com.returensea: DEBUG
  # 本地测试：与 gateway、core 统一输出到 ai-agent-platform/logs/ 便于排查
  file:
    name: ../../logs/legacy-dummy.log
